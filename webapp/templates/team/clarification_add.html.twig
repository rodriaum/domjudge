{% extends 'team/base.html.twig' %}

{% block title %}Enviar pedido de esclarecimento - {{ parent() }}{% endblock %}

{% block content %}
    <div class="mt-3 container">
        <div class="card card-body clarificationform">
            <h1 class="text-center">Enviar Pedido de Esclarecimento</h1>

            {{ form_start(form) }}
            {{ form_row(form.recipient) }}
            {{ form_row(form.subject) }}
            {{ form_row(form.message) }}
            <div class="mb-3">
                <div class="card">
                    <div class="card-body">
                        <label class="rendered-preview-badge">pré-visualização renderizada</label>
                        <div id="messagepreview">Comece a escrever para ver uma pré-visualização da sua mensagem</div>
                    </div>
                </div>
            </div>
            <div>
                <button type="submit" name="submit" class="btn btn-primary">
                    <i class="fas fa-envelope"></i> Enviar
                </button>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

{% block extrafooter %}
    <script>
        setupPreviewClarification($('#team_clarification_message') , $('#messagepreview'), true);
    </script>
{% endblock %}